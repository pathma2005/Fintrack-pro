# TODO for Adding Full Cover Background Image and Glass Effect to SignupSignin Component

## Tasks
- [x] Modify `.signupwrapper` class in `my-react-app/src/Components/SignupSignin/style.css` to add full-cover background image and glass-like transparency effect.
- [x] Test the component to ensure the image covers fully and the glass effect is visually appealing.

## Notes
- Background image: `img2.jpg` from `my-react-app/src/assets/`.
- Glass effect: Semi-transparent background with backdrop blur.
- Preserve existing styles (box-shadow, border, etc.).
